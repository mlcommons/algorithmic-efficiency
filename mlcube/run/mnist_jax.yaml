schema_type: mlcube_invoke
schema_version: 1.0.0

task_name: train

input_binding:
        flagfile: $WORKSPACE/mnist_jax.cfg

output_binding:
        log_dir: $WORKSPACE/mnist_jax_logs
        data_dir: $WORKSPACE/mnist_jax_dataset